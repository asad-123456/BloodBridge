# Frontend environment variables
# These values are safe to expose to the browser only if they are non-secret.
# Never store API keys, tokens, or private credentials in frontend env files.
VITE_APP_NAME=BloodBridge
VITE_API_BASE_URL=http://localhost:8000

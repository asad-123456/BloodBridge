# Admin has no database table. Credentials are hardcoded via ADMIN_EMAIL /
# ADMIN_PASSWORD in settings (same pattern as MediConnect), and identity is
# carried entirely in the JWT payload ({"role": "admin", "id": "admin"}).
